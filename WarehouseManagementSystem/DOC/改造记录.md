# 改造记录

| 日期 | 改造内容清单 | 备注 |
| :--- | :--- | :--- |
| 2026-05-14 | 1. 统一数据库连接配置方案<br>2. 配置局域网 IP 访问<br>3. 修复 AGV 指令收件箱数据库连接报错 (Shared Memory Provider 错误)<br>4. 排查持续报错问题 | 1. 移除了 appsettings.Production.json 中的冗余连接字符串。<br>2. 确认了 Program.cs 监听 0.0.0.0。<br>3. 将连接字符串改为 `Server=127.0.0.1` 并显式关闭 `Encrypt`，以强制 TCP 连接并避免管道错误。<br>4. **注意：** 修改后必须彻底关闭并重启程序才能生效。 |
| 2026-05-23 | 1. 新增系统日志中心 (Log Center) 功能，提供历史日志管理及多维度筛选能力。 | 1. 后端新增 `ApiLogController.cs` 接口，支持安全防遍历读取。<br>2. 前端新增 `LogCenterView.vue`、路由配置及中英文国际化适配。 |

---

- 【改造日期】：2026-05-23
- 【改造模块/功能】：系统日志中心 (Log Center)
- 【改造清单】：
  1. **后端 API 控制器**: 新增 `ApiLogController.cs`，提供 `/api/log/files` 查询服务器 `Logs` 文件夹下所有天数的日志文件列表，并提供 `/api/log/content` 用以安全读取指定文件内容。加入严格的路径越权防范（防路径遍历）与 `FileShare.ReadWrite` 共享读写，规避与正在写入的 Serilog 发生文件锁死异常。
  2. **前端路由与导航**: 
     - 在 `router/index.ts` 注册 `/logs` 子路由与页面懒加载。
     - 在 `MainLayout.vue` 的系统管理 (System) 子菜单下新增“日志中心 (Log Center)”菜单项，并设为由 `SETTINGS` 权限控制访问。
  3. **国际化字典适配**: 在 `zh-CN.ts` 与 `en-US.ts` 翻译字典中补全所有系统日志中心所用的组件文本（标题、下拉、过滤、状态提示、容量格式化、加载等）。
  4. **日志交互服务 API**: 新增 `src/services/log.ts`，定义 TS 类型并集成 axios 请求。
  5. **高级控制台组件**: 新增 `LogCenterView.vue` 单文件组件：
     - 提供下拉选择器供用户灵活、手工选择任何一天的日志，后端自动判断物理路径。
     - 设为默认仅加载最新 1000 行，以确保界面渲染速度，并支持一键“加载完整日志”。
     - 包含前端秒级模糊关键字搜索，及 DEBUG/INFO/WARN/ERROR 严重级别快捷筛选。
     - 仿真终端采用 Dark Terminal 现代美学设计，支持自适应系统亮暗色主题，对日志级别高亮渲染。
     - 具备实时 Auto Refresh 定时刷新模式与全屏（Fullscreen）放大沉浸查看。

## 2026-06-25

* **[储位标准模型]**：为 `RCS_Locations` 对应储位模型新增 `LaneCode` 与 `DepthIndex` 字段，并将深位储位前后关系统一改为基于结构化字段判断，不再依赖储位名称解析。
* **[储位后端接口]**：更新储位创建、编辑、详情、列表及批量导入接口，统一接收并返回 `LaneCode`、`DepthIndex`，同时补充库道编号必填与深度序号大于 0 的校验。
* **[仓储解耦改造]**：新增储位仓储层 `LocationRepository`，将储位分页查询、详情、增删改、批量清空、批量锁定等数据库访问从服务层/控制器中物理隔离，满足二次开发解耦规范。
* **[寻库推荐逻辑]**：在储位服务层新增标准目标储位推荐逻辑，按 `LaneCode` 分组、按 `DepthIndex` 判断可达性与推荐顺序，实现“同库道先放最里面，外侧未占用时内侧不可达”的规则。
* **[任务终点选择]**：调整任务可用储位接口，改为返回标准推荐结果，补充库道编号、深度序号、锁定状态、物料信息、可达状态和推荐顺序，供任务创建页与后续项目直接复用。
* **[储位维护前端]**：重构新建/编辑储位页面，移除旧的模糊 `depth` 录入方式，新增“库道编号”“深度序号”输入项，仅保留现场施工人员需要填写的标准字段组合。
* **[储位列表与详情]**：更新储位列表、详情弹窗、前端类型定义与状态存储，统一展示 `LaneCode`、`DepthIndex` 与“信号请求点”，保证标准储位信息在页面各处可见。
* **[批量导入模板]**：同步修改储位 Excel 导入模板、导入预览和解析逻辑，模板字段改为包含“库道编号”“深度序号”，并按新字段执行导入前校验。
* **[储位创建修复]**：修复新建储位时报错“必须声明标量变量 `@CreatedTime`”的问题，在储位新增 SQL 中显式传入 `CreatedTime = DateTime.Now`，避免 Dapper 插入缺参导致创建失败。
* **[批量导入修复]**：补全 `LocationsView.vue` 中缺失的 `handleConfirmImport` 实现，修复导入预览弹窗点击“确认导入”后未实际调用后端接口的问题。
* **[导入交互优化]**：批量导入成功后自动刷新储位列表；当导入全部成功时关闭预览弹窗并清空预览数据；若存在失败项则保留后端错误提示，便于现场定位问题。
* **[字段文案调整]**：将页面与导入模板中的“挂作点”统一改名为“信号请求点”，并同步更新中英文国际化文案。
* **[前端缺失依赖]**：补充安装 `echarts` 与 `vue-echarts` 到 `frontend` 依赖，修复生产构建中图表模块缺失问题。
* **[前端构建环境]**：通过显式注入本机 Node 运行时路径完成 `frontend` 依赖安装与构建，避免本机未配置 `node`/`npm` PATH 导致的构建中断。
* **[前端打包产物]**：成功执行 `frontend` 的 `vite build`，并将最新静态资源输出到项目 `wwwroot`，包含新的储位管理页面、任务页面及入口 `index.html`。
---

## 2026-06-26

* **[AGV 指令服务解耦]**：将 `IAgvIntegrationService` 生命周期调整为 `Scoped`，并在后台收件箱处理服务中改为按轮询作用域解析服务实例，修复单例服务直接依赖 `IUserTaskCreationService` 导致的依赖注入构造异常。
* **[PDA 绑定界面]**：新增自适应 `PdaBindingView.vue` 页面、首页入口、侧边菜单、路由与中英文国际化，支持现场人员选择 MES 生效工单、扫码录入并二次确认提交绑定。
* **[PDA 绑定后端]**：新增 `ApiPdaBindingController`、`PdaBindingService`、`PdaBindingRepository` 与 `RCS_PdaTaskBinding` 模型，提供工单下拉查询、扫码绑定落库及统一返回结构，并补充 `RCS_PdaTaskBinding` 建表 SQL 脚本。
* **[平板任务规则封装]**：在 `UserTaskCreationService` 中新增平板送料任务的标准路由解析入口，统一汇总工单、扫码绑定记录、默认系统配置及临时备注提示来解析起点/终点，后续现场只需修改单一规则方法即可切换正式业务逻辑。
* **[平板任务上下文透传]**：为统一任务创建请求补充 `OrderNumber`、`ScanCode` 等上下文字段，并在 PDA 绑定创建任务时显式透传工单号与扫码值，同时将绑定备注改为可承载临时 `source/target` 路由提示的格式，便于现场规则渐进落地。
* **[完整编译复核]**：使用独立输出目录执行完整 `dotnet build`，绕开运行中进程对默认 `bin` 目录的占用，确认本轮后端代码编译结果为 `0` 错误，仅保留目标框架与依赖版本相关警告。
* **[工单托盘号贯通]**：为 MES 工单请求模型、`RCS_WorkOrder` 映射与工单入库逻辑补充 `PalletNumber` 字段，并在工单校验时改为强制校验托盘号，确保 PDA 绑定和完工回传可直接取到“亚批号 + 托盘号”。
* **[PDA 绑定表正式化]**：扩展 `RCS_PdaTaskBinding` 结构，新增 `PalletNumber`、`Barcode`、`ExternalTaskType`、`FeedbackStatus`、`FeedbackTime`、`FeedbackError` 字段，并将建表 SQL 改为兼容“首次建表 + 已有旧表补字段”两种场景。
* **[PDA 任务完成回传]**：在 `RcsWmsTaskHostedService.UpdateTaskStatusAsync` 中为平板送料专用任务类型 `101` 增加完成回传触发逻辑，任务完结后按 `orderNumber + palletNumber + items[{barcode}]` 结构入统一 AGV 出站队列，并同步回写绑定记录回传状态。
* **[PDA 回传事件通道]**：在 `AgvOutboundInteractionService` 中新增 `PdaBindingCompleted` 出站事件类型与独立接口地址键 `AgvUpstream:PdaBindingCompletedEndpoint`，用于把 PDA 绑定完成报文异步发送给对方上位机，避免与原有“物料达到生产线信息”事件混用。
* **[完整编译复核补充]**：再次使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_pda_feedback`，确认本轮“绑定表 + 完成回传”改造后端编译结果为 `0` 错误。
* **[PDA 扫码字段纠正]**：根据现场规则，将平板绑定输入从单一“扫码内容”修正为两个独立字段：`PalletNumber`（托盘号）与 `Barcode`（SSCC码），并撤回上一轮误接到 `RCS_WorkOrder` / MES 工单入库模型中的托盘号依赖。
* **[PDA 绑定接口调整]**：更新 `ApiPdaBindingController`、`PdaBindingService` 与前端 `pdaBinding` 服务请求模型，改为统一提交 `orderNumber + palletNumber + barcode`，同时把重复校验调整为按“工单号 + 托盘号 + SSCC码”组合判重。
* **[PDA 页面交互调整]**：重构平板绑定页面为“先扫托盘号，再扫SSCC码”的双输入流程，补充中英文文案、确认弹框与扫码焦点切换逻辑，适配现场连续扫码操作。
* **[完整编译复核补充二]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_pda_split_scan`，确认本轮“托盘号/SSCC拆分录入”调整后端编译结果为 `0` 错误。
* **[MES 完成反馈范围收敛]**：调整 `RcsWmsTaskHostedService.UpdateTaskStatusAsync` 的“作业完成反馈”触发范围，仅允许统一任务来源标记为 `MES` 的任务在终态时调用 `NotifyJobCompletedAsync`；平板任务继续仅走 `PdaBindingCompleted` 专用回传，人工任务不再误发 MES 完工接口。
* **[完整编译复核补充三]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_mes_feedback_scope`，确认本轮“MES 完成反馈范围收敛”调整后端编译结果为 `0` 错误。
* **[MES 默认终点配置]**：在系统设置中新增 `MesTaskType2TargetGroup` 与 `MesTaskType4TargetGroup` 两个业务参数，分别用于配置 MES 任务类型 `2` 与 `4` 在未传 `toStation` 时的默认终点区域（对应 `RCS_Locations.Group`）。
* **[MES 终点自动寻址]**：在 `UserTaskCreationService` 中新增 MES 外部任务路由解析入口，当类型 `2/4` 未传 `toStation` 时，自动按配置区域筛选“无任务、未锁定、未占用、已启用”的储位，并复用标准库道/深度推荐规则选择目标储位写入 `RCS_UserTasks`。
* **[系统设置前端贯通]**：更新 `SettingView.vue` 与中英文国际化文案，在业务参数页增加“MES类型2默认终点区域”“MES类型4默认终点区域”输入项，便于现场通过页面维护默认终点分组，无需改代码。
* **[MES 设置页前端发布]**：成功执行 `frontend` 的 `vite build`，并将包含 MES 类型 `2/4` 默认终点区域设置项的新前端资源发布到项目 `wwwroot`，现场刷新页面后即可在系统设置中维护对应分组。
* **[PDA 独立页面入口]**：将 `pda-binding` 调整为独立路由页面，不再嵌套在主后台布局中显示，保留登录校验并补充“返回首页”入口，更适合平板设备直接访问与扫码操作。
* **[储位分组下拉配置]**：为储位接口新增分组列表查询能力，设置页改为从 `RCS_Locations.Group` 动态加载下拉选项，替代手工输入默认终点区域名称，降低现场误填风险。
* **[MES 任务名称文案修正]**：将设置页参数文案按现场任务口径调整为“2. 取空托”“4. 取满bin(取空bin)”对应默认终点分组，避免继续使用抽象的类型编号名称造成理解偏差。
* **[前端发布补充]**：重新执行 `frontend` 的 `vite build`，将独立 `PdaBinding` 页面和基于储位分组下拉的最新设置页资源发布到项目 `wwwroot`。
* **[分组下拉取值修正]**：修正设置页读取储位分组接口时的前端响应解包逻辑，解决下拉框因重复读取 `data` 层级而显示“暂无数据”的问题，并重新发布最新前端资源。
---

## 2026-07-01

* **[PLC DB1标准点位初始化]**：在 PLC 信号服务层新增 `192.168.0.100 / DB1` 标准模板初始化能力，按现场 DB1 点位一次性幂等导入 `RCS_PlcDevice` 与 `RCS_PlcSignal`，并将重复名称信号改为“区域/方向/信号名”的唯一命名格式，避免同设备多 IO 分区下信号重名导致页面串列或后续任务命中错误。
* **[PLC初始化接口]**：在 `ApiPlcSignalController` 新增 `initialize-default-db1` 接口，支持页面一键导入西门子 `DB1` 标准设备与信号模板，避免现场人工逐条维护偏移量、DB块和写入权限。
* **[中控写入权限收口]**：将“只有中控写 PLC 的信号允许手动写入和重置”落实到服务层与状态控制接口，新增统一写入方标识 `WMS` 作为后端铁规则，防止通过绕过前端的方式误操作 PLC 原生只读/只写点。
* **[中控心跳切换]**：将 PLC 心跳配置扩展为支持按信号备注或信号名称定位心跳点，新增 `192.168.0.100 / DB1 / 心跳/中控心跳` 配置，并将心跳周期从 1 秒调整为 0.5 秒闪烁，满足现场“中控心跳”要求。
* **[PLC显示页手动控制限制]**：更新 `plc/signal-display` 页面，仅当信号写入方为 `WMS`（中控写 PLC）时才显示置位按钮；其余信号统一显示禁用提示，避免操作员误以为所有点位都允许人工干预。
* **[PLC管理页初始化入口]**：更新 `plc/signal-management` 页面，新增“初始化DB1模板”入口，支持一键生成 `192.168.0.100 / DB1` 的标准设备与信号数据并自动刷新列表。
* **[PLC前端文案与发布]**：补充中英文国际化文案，明确“仅中控写PLC信号允许手动写入或重置”，并执行 `frontend` 的 `vite build` 将最新 PLC 页面资源发布到 `wwwroot`。
* **[PLC完整编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_plc_db1`，确认本轮 PLC DB1 初始化、心跳与权限收口改造后端编译结果为 `0` 错误，仅保留项目现有框架与空引用相关警告。
* **[PLC初始化连接修复]**：修复 `initialize-default-db1` 初始化接口首次执行时报错“Invalid operation. The connection is closed.”的问题，在 `PlcSignalService.InitializeDeviceSignalsAsync` 中于开启事务前显式打开数据库连接，保证幂等初始化流程可正常创建事务并写入设备/信号数据。
* **[PLC修复编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_plc_db1_fix`，确认本轮初始化连接修复后端编译结果为 `0` 错误，仅保留项目现有警告。
* **[PLC信号管理备注隐藏]**：更新 `PlcSignalManagementView.vue`，在信号列表表格列定义中将“备注”列移出，避免排版重合，美化表格展示。
* **[PLC任务管理删除功能]**：在 `PlcTaskManagementView.vue` 任务表格操作列中添加删除按钮，仅对未发送任务开放，同时补全中英文对应的 `confirmDeleteTask` 国际化词条；更新操作列宽度以良好兼容多按钮排布。
* **[PLC任务前端发布与整编译]**：执行前端 `vite build` 重新构建并发布到 `wwwroot`，再次通过 `dotnet build` 确认项目编译正常。
* **[PLC任务删除类型异常修复]**：修复 `ApiAutoPlcTaskController.DeleteTask` 中对 dynamic 类型字段 `task.IsSend` 进行直接 bool 类型评估时抛出 `RuntimeBinderException` 的问题，改用 `Convert.ToInt32(task.IsSend) == 1` 安全转换，避免接口返回 500 内部服务错误。
* **[导航栏PDA扫码入口隐藏]**：更新 `MainLayout.vue`，从左侧导航菜单中移除“PDA扫码绑定”项，保留其路由入口，改由仪表盘直接跳转，避免菜单入口重复冗余。
---

## 2026-07-02

* **[AGV任务下发链路收敛]**：为 `IAgvIntegrationService` 新增统一链路说明方法 `DescribeAgvCommandDispatchFlow`，将 `ReceiveAgvCommand -> EnqueueAgvCommandAsync -> AgvCommandInboxProcessorService -> ProcessPendingCommandInboxAsync -> ProcessSingleInboxAsync -> IUserTaskCreationService.CreateTaskAsync` 收敛为单一口径，避免控制器注释、日志和后台消费入口各写各的。
* **[ReceiveAgvCommand可读性增强]**：更新 `IntegrationController.ReceiveAgvCommand` 的文档注释与接收日志，让接口入口一眼能看出“当前只负责入收件箱，最终统一归口到 `IUserTaskCreationService.CreateTaskAsync`”，降低后续二开排查任务去向时的理解成本。
* **[收件箱到统一任务请求映射集中化]**：在 `AgvIntegrationService` 中提取 `BuildUnifiedMesTaskCreationRequest`，将 AGV 收件箱明细到 `UserTaskCreationRequest` 的映射从处理循环中收口，保证不同任务类型虽然逻辑分流不同，但进入统一任务创建入口前的组装责任清晰集中。
* **[AGV链路编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_dispatch_flow`，确认本轮“任务下发全链路收敛”改造编译结果为 `0` 错误，仅保留项目现有框架与空引用相关警告。
* **[AGV收件箱连接状态修复]**：修复 `ProcessPendingCommandInboxAsync` 复用同一数据库连接时，`ProcessSingleInboxAsync` 再次强制 `Open()` 导致报错“`The connection was not closed. The connection's current state is open.`”的问题；改为仅在连接未打开时才打开，并避免内层误关闭外层仍在复用的连接。
* **[AGV连接修复编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_inbox_connection_fix`，确认本轮收件箱连接状态修复后端编译结果为 `0` 错误，仅保留项目现有警告。
* **[AGV指令明细中文化]**：更新 `integration-data` 页面 AGV 指令明细展示，将 `taskType` 从纯数字改为“1-运料 / 2-取空托 / 3-送空bin / 4-取满bin / 5-退料”中文标签，并补充 `requestCode`、`UserTaskId`、明细处理状态与错误信息显示，便于现场直接对照每条明细是否成功生成任务。
* **[AGV明细状态独立化]**：扩展 `RCS_AgvCommandInboxItems` 结构，新增 `RequestCode`、`ProcessStatus`、`ErrorMsg`、`UserTaskId`、`UpdateTime`、`ProcessTime` 字段，让每条明细可独立标记“待转换 / 已生成任务 / 转换失败”，避免仅靠主收件箱状态无法区分单条明细是否真正业务通过。
* **[AGV任务归口点注释增强]**：在 `AgvIntegrationService.BuildUnifiedMesTaskCreationRequest` 补充 Why 注释，明确收件箱消费者只负责逐条消费与状态回写，而不同任务类型的起点/终点判断及最终插入 `RCS_UserTasks` 的统一归口仍在 `UserTaskCreationService.BuildExternalTaskDraftAsync / CreateTaskAsync`，方便后续集中改造任务创建规则。
* **[AGV明细状态编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_item_status`，确认本轮“明细中文显示 + 明细状态独立化 + 统一任务归口注释增强”改造后端编译结果为 `0` 错误，仅保留项目现有 warning。
* **[AGV明细增量升级脚本]**：新增 `Db/Sql/20260702_Alter_AgvCommandInboxItems_AddStatusFields.sql`，支持对已有 `RCS_AgvCommandInboxItems` 生产表直接补充 `RequestCode`、`ProcessStatus`、`TaskStatus`、`ErrorMsg`、`UserTaskId`、`UpdateTime`、`ProcessTime` 字段，并自动回填已存在 `RCS_UserTasks` 的历史关联关系，无需重建表。
* **[AGV明细执行状态同步]**：在 `UserTaskCreationService` 创建任务成功时返回初始 `taskStatus`，并在 `AgvIntegrationService` 落库明细时同步写入 `TaskStatus`；同时在 `RcsWmsTaskHostedService.UpdateTaskStatusAsync` 中新增按 `requestCode` 反写 `RCS_AgvCommandInboxItems.TaskStatus` 的逻辑，使明细执行状态与 `RCS_UserTasks.taskStatus` 保持一致。
* **[AGV明细状态同步编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_taskstatus_sync`，确认本轮“增量脚本 + 明细执行状态同步”改造后端编译结果为 `0` 错误，仅保留项目现有 warning。
* **[AGV明细状态前端发布]**：复核确认 `integration-data` 指令明细页源码已包含“明细处理状态”和“任务执行状态”列，本次重新执行 `frontend` 的 `vite build` 并发布到 `wwwroot`；最新静态资源时间已更新为 `2026-07-02 09:47:23`，现场刷新页面后即可看到状态列。
* **[AGV明细状态补偿同步]**：补充 `RcsUserTaskService.RepairInboxItemTaskStatusesAsync` 与 `RcsWmsTaskHostedService` 轮询补偿逻辑，除实时按 `requestCode` 回写外，新增按 `UserTaskId` 优先、`RequestCode` 兜底的状态修复机制，解决历史终态任务或服务重启遗漏导致 `RCS_AgvCommandInboxItems.TaskStatus` 停留在 `-1/NULL` 的问题。
* **[AGV任务类型判断入口收口]**：在 `UserTaskCreationService` 中新增 `ResolveExternalTaskBehavior`，将外部任务类型 `1/2/3/4/5/101` 对应的内部 `taskType` 与是否走特殊路由解析集中到单一方法，便于后续围绕不同任务类型调整 `RCS_UserTasks` 插入规则与起终点判断。
* **[AGV状态补偿编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_status_repair`，确认本轮“明细状态补偿同步 + 任务类型判断入口收口”改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[MES任务类型单方法封装]**：按二开维护诉求重构 `UserTaskCreationService`，将 MES 外部任务 `1/2/3/4/5` 的内部 `taskType` 判定、起点沿用、终点直传或默认区域补位逻辑统一收口到 `BuildMesTaskDraftByTaskTypeAsync` 一个方法中；后续若要改某个任务类型如何插入 `RCS_UserTasks`，优先只需维护这一处。
* **[MES单方法封装编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_mes_single_method`，确认本轮“MES任务类型单方法封装”改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[AGV任务下发链路瘦身]**：按当前项目“小逻辑直接落任务”的诉求，新增 `ReceiveAndCreateUserTasksAsync` 同步主线，将“写入 `RCS_AgvCommandInbox / RCS_AgvCommandInboxItems` + 逐条拆分生成 `RCS_UserTasks` + 回写明细状态”收回同一事务流程，不再依赖后台轮询二次消费才能落任务。
* **[接口主线切换]**：更新 `IntegrationController.ReceiveAgvCommand`，改为直接调用同步主线；链路描述同步收敛为 `ReceiveAgvCommand -> ReceiveAndCreateUserTasksAsync -> BuildMesTaskDraftByTaskTypeAsync -> IUserTaskCreationService.CreateTaskAsync`，便于后续排查时一眼看清入口和最终任务生成点。
* **[后台收件箱消费者停用]**：从 `Program.cs` 中移除 `AgvCommandInboxProcessorService` 的 HostedService 注册，并在原类中显式标记“已停用，仅保留历史追溯”，避免同一批 AGV 指令再被异步重复消费，减少无谓架构复杂度。
* **[AGV瘦身编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_sync_slim`，确认本轮“同步接收即创建任务”的架构瘦身改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[MES任务类型与NDC位置联动]**：在 `BuildMesTaskDraftByTaskTypeAsync` 中补充与 `RcsWmsTaskHostedService` 的位置联动规则：类型 `1/3/5` 在插入 `RCS_UserTasks` 前先校验起点、终点都存在于 `NdcLocation(NodeRemark)`；类型 `2/4` 先校验起点存在，再由系统自动查找终点，避免下发到 NDC 阶段才因位置缺失被取消。
* **[MES固定终点区域找位]**：将类型 `2` 的默认终点区域固定为常量“空托回收处”，类型 `4` 固定为“BIN暂存间”；查找逻辑按“同区域内先选深度最里面、未锁定、空闲、未被未完成任务占用且不与起点相同”的储位返回 `NodeRemark`，保证生成的 `RCS_UserTasks.targetPosition` 可被后续 NDC 下发链路直接使用。
* **[MES路由规则编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_mes_route_rules`，确认本轮“任务类型与 NDC 位置联动 + 固定区域自动找终点”改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[手动建任务起点限制放开]**：调整 `frontend/src/views/TaskCreateView.vue`，先移除 `tasks/create` 页面起点选择对“空置/锁定”的前端拦截，仅保留“不能与目标位选同一储位”的基础防呆；这样现场可直接手动指定起点，同时不影响终点侧原有的可达性与推荐规则。
* **[前端重新构建发布]**：重新执行 `frontend` 的 `vite build`，最新静态资源已成功输出到 `wwwroot`；本次已包含 `tasks/create` 起点限制放开等最新页面改动，可直接在现场刷新浏览器验证。
* **[AGV明细失败不中断后续]**：调整 `ReceiveAndCreateUserTasksAsync` 的逐条建任务逻辑，改为单条明细失败时仅回写该条 `ErrorMsg/ProcessStatus` 并继续处理后续明细，不再因前一条失败导致整批后续明细全部停在“待处理”；主收件箱最终按“全部成功 / 部分失败 / 全部失败”汇总结果回写。
* **[AGV局部失败编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_partial_continue`，确认本轮“单条失败不中断后续明细”改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[MES/AGV联调测试储位脚本]**：新增 `Db/Sql/20260702_Insert_MesAgvTestLocations.sql`，按当前 `BuildMesTaskDraftByTaskTypeAsync` 规则一次性准备类型 `1/2/3/4/5` 所需的测试储位数据；其中包含“已知起终点校验点位”“空托回收处自动找位点位”“BIN暂存间自动找位点位”，可直接用于当前 AGV 指令明细联调。
* **[任务类型口径对齐]**：按现场最新规则调整 `UserTaskCreationService`：`MES` 下发的 `taskType` 改为原样写入 `RCS_UserTasks.taskType`，不再映射到旧的内部 `In/Out/Transfer` 语义；同时额外保留 `100=人工任务`、`101=到线边任务` 两个内部类型，便于后续区分人工建任务与平板送料任务。
* **[到线边任务判断同步修正]**：同步调整 `RcsWmsTaskHostedService` 中“物料到线边到达”“平板送料任务识别”的判断条件，改为按 `taskType=101` 判断，不再继续依赖旧的 `taskType=In` 语义，避免任务类型口径切换后回传链路误判。
* **[任务类型口径编译复核]**：使用独立输出目录执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_tasktype_passthrough_fix`，确认本轮“MES任务类型原样落库 + 人工/到线边内部类型保留”改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[任务类型中文描述修正]**：更新 `frontend/src/constants/taskType.ts`，将任务管理、任务详情、任务创建页共用的 NDC 任务类型文案改为与最新口径一致：`1=运料`、`2=取空托`、`3=送空bin`、`4=取满bin`、`5=退料`、`100=人工任务`、`101=到线边`，避免页面继续显示旧的“入库/出库/母托盘/搬运”描述。
* **[任务类型前端发布]**：重新执行 `frontend` 的 `vite build` 并发布到 `wwwroot`，本次已包含任务类型中文描述修正；刷新页面后任务列表、任务详情等页面将显示新的中文名称。
* **[RCS任务状态同步收口]**：重构 `RcsWmsTaskHostedService.UpdateTaskStatusAsync`，将主循环职责收敛为“补偿明细状态、筛选真实状态变化、分发统一处理入口”，不再在轮询方法内散落执行任务回写、明细同步、库位释放和上位机回传等副作用逻辑。
* **[任务状态统一处理入口]**：在 `RcsWmsTaskHostedService` 中新增统一方法承接任务状态变化后的处理，集中完成 `RCS_UserTasks` 状态更新、`RCS_AgvCommandInboxItems.TaskStatus` 同步、机器人编号回写、异常日志记录及后续交互触发，便于后续二开只围绕单一方法维护状态流转规则。
* **[库位锁释放规则独立化]**：将 `PickDown` 后提前释放起点锁、任务完成/取消/改道后释放起终点锁的规则独立封装，避免状态获取逻辑和库位资源处理逻辑混写，提高 `UpdateTaskStatusAsync` 可读性。
* **[状态收口编译复核]**：修正统一处理方法参数类型为项目实际使用的 `NdcTaskMove` 后，执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_update_taskstatus_refactor`，确认本轮 `UpdateTaskStatusAsync` 收口改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[AGV出站线程逻辑瘦身]**：按“查表 -> 按类型分发接口 -> 回写结果”的维护思路重构 `AgvOutboundInteractionService.ProcessPendingAsync`，将原先混合在一起的待发送查询、事件分发、发送执行、结果回写拆成显眼的主干方法，降低后续二开理解成本。
* **[出站任务分发入口收口]**：新增 `LoadPendingOutboundTasksAsync`、`DispatchOutboundTaskAsync`、`UpdateOutboundTaskResultAsync` 三个统一方法，把 `RCS_AgvOutboundQueue` 的后台消费流程收口为单一主线，便于后续围绕 `EventType` 扩展或切换到其他出站任务表。
* **[接口类型路由简化]**：新增 `ResolveDispatchTarget`，将“根据表中事件类型选择接口地址”的逻辑直接显式化，保持与现场理解一致，不再让维护者在多个 helper 之间跳转理解发送目标。
* **[AGV出站瘦身编译复核]**：执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_outbound_slim`，确认本轮出站线程瘦身改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[MES上报接口地址配置]**：在 `AgvUpstream` 配置节点中补充 MES 对接地址：`MaterialArrivedEndpoint=http://10.200.178.89:9081/MES/AGV/matDeliv`、`SafetySignalEndpoint=http://10.200.178.89:9081/MES/AGV/safeSignal`、`JobCompletedEndpoint=http://10.200.178.89:9081/MES/AGV/taskComplete`，确保物料到达、安全信号、作业完成反馈可按当前出站线程直接投递。
* **[多环境配置同步]**：同步更新 `appsettings.json`、`appsettings.Development.json`、`appsettings.Production.json` 三份配置，避免本地调试、开发环境与生产环境读取到不同的 MES 接口地址。
* **[AGV出站队列前端现状确认]**：复核确认 `RCS_AgvOutboundQueue` 已具备前后端展示链路，后端 `ApiIntegrationDataController` 已提供查询/新增/编辑/删除接口，前端 `integrationData.ts` 与 `IntegrationDataView.vue` 已存在 `outboundQueue` 页签及查询逻辑，可在 `integration-data` 页面展示出站队列历史数据。
* **[MES类型2/4仅锁终点]**：调整 `UserTaskCreationService` 任务草稿锁定粒度，新增源位/目标位独立锁定控制；其中 `BuildMesTaskDraftByTaskTypeAsync` 对 MES 类型 `2`、`4` 自动找位成功后，仅开启目标储位锁定，不再同时锁定起点。
* **[任务锁定粒度收口]**：更新 `LockTaskLocationsAsync`，改为按草稿中的“锁起点 / 锁终点”标志分别决定实际锁定哪些 `NodeRemark`，为后续不同任务类型按需锁源、锁目标或同时锁两端预留统一入口。
* **[MES仅锁终点编译复核]**：执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_mes_lock_target_only`，确认本轮“MES类型2/4只锁终点”改造编译结果为 `0` 错误，仅保留项目现有 warning。
* **[安全交互直联化]**：将装货前安全交互从 `RCS_AgvOutboundQueue` 异步出站队列中拆出，改为由 `AciDataEventHandler` 通过新的 `IAgvSafetyInteractionService` 在 `HandleLoadHostSyncronisationEvent` 内实时直联 MES 安全接口，并仅在返回 `safeFlag=Y` 时回复 NDC 放行。
* **[安全握手状态表]**：新增 `RCS_AgvSafetyHandshake` 模型、仓储与建表脚本 `Db/Sql/20260702_Create_AgvSafetyHandshake.sql`，按“任务号 + 房间”唯一维度保存最近一次安全请求时间、响应结果、`safeFlag`、错误信息与响应报文，用于支撑 30 秒节流重试和现场历史排查。
* **[安全交互节流重试]**：在 `AgvSafetyInteractionService` 中集中实现“30 秒内不重复请求、返回 `N` 则继续等待、接口异常则记录错误并等待下一次 NDC 循环重试”的规则，避免安全交互被误当作一次性上报而反复插入普通出站队列。
* **[安全出站职责收缩]**：从 `IAgvOutboundInteractionService` 移除 `NotifySafetySignalAsync` 及 `SafetySignal` 路由，明确 `RCS_AgvOutboundQueue` 仅承载物料到线边、作业完成、PDA 回传等一次性异步上报，不再混用实时联锁类安全握手。
* **[安全交互编译复核]**：执行 `dotnet build WarehouseManagementSystem.csproj -o .codex_build_agv_safety_direct`，确认本轮“安全交互直联 + 状态表留痕”改造编译结果为 `0` 错误，仅保留项目现有 warning。
---
